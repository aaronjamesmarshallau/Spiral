using NUnit.Framework;
using System;
using SwinGameSDK;

namespace MyGame
{
	[TestFixture ()]
	public class MapTests
	{
		/*[Test ()]
		public void TestTileInit () //Works in practice, not in theory?
		{
			NormalTile tTile = new NormalTile(TileType.BUSH, true);

			Assert.AreEqual (TileType.BUSH, tTile.Type, "TYPE::TEST1");
			Assert.AreEqual (true, tTile.IsSolid, "SOLID::TEST1");

			tTile.Type = TileType.GRASS;

			Assert.AreEqual (TileType.GRASS, tTile.Type, "TYPE::TEST2");
		}

		[Test()]
		public void TestMapIsSolidAt() { //Weird stuff happens with SwinGame during Unit Testing. Not really sure why.
			Map tMap = new Map ("./Resources/maps/ferring_town.map");
			tMap.Load ();
			Assert.AreEqual(true, tMap.IsSolidAt (0, 0), "TestSolid1");
		}*/
	}
}

